
language: java
jdk: oraclejdk8

install:
  - nvm install 7.7.1
  - npm install

branches:
  only:
    - develop
script:
  - cd backend
  - ./gradlew bootRepackage

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

